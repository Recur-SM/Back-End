server:
  port: ${PORT:8080}

spring:
  datasource:
    url: ${MYSQL_URL:${DATABASE_URL}}
    username: ${MYSQLUSER:${MYSQL_USER}}
    password: ${MYSQLPASSWORD:${MYSQL_PASSWORD}}
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update